
spring:
  datasource:
#    url: jdbc:mysql://localhost:3306/watch_party_db
    url: jdbc:mariadb://localhost:3306/watch_party_db
    username: root
    password: password
    hikari:
      maximum-pool-size: 48
      minimum-idle: 10
      pool-name: custom-pool-name
  jpa:
    hibernate:
      ddl-auto: update
#    show-sql: true
#    open-in-view: false
  threads:
    virtual:
      enabled: true

  servlet:
    multipart:
      max-file-size: 5000MB
      max-request-size: 5000MB
#logging:
#  file:
#    path: ./logs
server:
  port: 8081	
  http2:
    enabled: true
#  tomcat:
#    threads:
#      min-spare: 100
#      max: 10000